{
  "name": "hyperfamily-companion",
  "version": "3.10.1-beta.4",
  "private": true,
  "description": "Android companion shell for HyperFamily Branch Monitor — loads the workstation's own interface over the store network",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor-community/file-opener": "^7.0.1",
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.1.2",
    "@capacitor/core": "^7.0.0",
    "@capacitor/filesystem": "^7.1.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "~5.6.0"
  }
}
